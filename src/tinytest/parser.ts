import * as vscode from "vscode";
import { TestingTools } from "../util";

export default async function parseTestsFromFile(
    testingTools: TestingTools,
    file: vscode.TestItem
): Promise<void> {
    return;
}
